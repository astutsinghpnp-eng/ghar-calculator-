# Local lead-capture data — contains real names/contact info, never commit this.
data/
*.jsonl

# Secrets — API keys live here locally, never in committed source.
# On Vercel, set the same variable names in the project's Environment
# Variables dashboard instead of shipping a .env file.
.env
.env.local

# Editor/OS junk
.DS_Store
Thumbs.db

# Local Claude Code settings (machine-specific tool-permission cache)
.claude/settings.local.json

node_modules/
